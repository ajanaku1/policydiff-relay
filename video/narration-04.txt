The model does not make the decision. Dahunsi approves the correction through a server-checked reviewer boundary. PolicyDiff locks a new delivery revision, sends one real Gmail message, and exposes a single-use acknowledgement link. The recipient confirms it once. Realtime state then marks delivery and acknowledgement complete, without a second send.
